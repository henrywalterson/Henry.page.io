{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "Personal portfolio site for a Product Designer",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build:analytics": "esbuild src/analytics.js --bundle --minify --outfile=public/js/analytics.js",
    "build": "npm run build:analytics"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "dotenv": "^16.4.5",
    "ejs": "^3.1.10",
    "express": "^4.19.2",
    "nodemailer": "^9.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "nodemon": "^3.1.4"
  }
}
